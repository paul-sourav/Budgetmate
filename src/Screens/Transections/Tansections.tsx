import {StyleSheet, View} from 'react-native';
import React from 'react';
import {Text} from '@ui-kitten/components';

const Tansections = () => {
  return (
    <View>
      <Text category="h2">Tansections</Text>
    </View>
  );
};

export default Tansections;

const styles = StyleSheet.create({});
